let character = "mario";
let age = 30;
let isBlackBelt = false;

// character = 20; // error
character = "luigi";

const circ = (diameter: number) => {
  return diameter * Math.PI;
};

console.log(circ(12));
